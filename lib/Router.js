import{jsx as _jsx}from"react/jsx-runtime";import{match}from"path-to-regexp";import{Children,isValidElement,useEffect,useState}from"react";import{EVENTS}from"./consts";import{getCurrentPath,getSearhcParams}from"./utils";import{RouterContext}from"./RouterContext";export function Router({routes=[],defaultComponent:DefaultComponent=()=>_jsx("h1",{children:"404"}),children}){const[pathname,setPathname]=useState(getCurrentPath());const[search,setSearch]=useState(getSearhcParams());const routesFromChildren=Children.map(children,child=>{if(isValidElement(child)){const{type,props}=child;let isRoute=false;if(typeof type==="function")isRoute=type.name==="Route";return isRoute?props:null}return null})||[];const routesToUse=routes.concat(routesFromChildren).filter(Boolean);useEffect(()=>{const onLocationChange=()=>{setPathname(getCurrentPath());setSearch(getSearhcParams())};window.addEventListener(EVENTS.PUSHSTATE,onLocationChange);window.addEventListener(EVENTS.POPSTATE,onLocationChange);return()=>{window.removeEventListener(EVENTS.PUSHSTATE,onLocationChange);window.removeEventListener(EVENTS.POPSTATE,onLocationChange)}},[]);let routeParams={};const Page=routesToUse.find(({path})=>{if(path===pathname)return true;const matchedURL=match(path,{decode:decodeURIComponent});const matched=matchedURL(pathname);if(!matched)return false;routeParams=matched.params;return true})?.Component||DefaultComponent;return _jsx(RouterContext.Provider,{value:{pathname,params:routeParams,search},children:_jsx(Page,{})})}